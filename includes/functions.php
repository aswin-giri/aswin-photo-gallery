<?php if ( ! defined( 'ABSPATH' ) ) exit;

function apg_sanatize_input(){

	/*
	sanitize_email()
	sanitize_file_name()
	sanitize_html_class()
	sanitize_key()
	sanitize_meta()
	sanitize_mime_type()
	sanitize_option()
	sanitize_sql_orderby()
	sanitize_text_field()
	sanitize_title()
	sanitize_title_for_query()
	sanitize_title_with_dashes()
	sanitize_user()
	esc_url_raw()
	wp_filter_post_kses()
	wp_filter_nohtml_kses()
	esc_html( $title );
	esc_url( $great_user_picture_url );
	esc_js( $custom_js );
	esc_attr( $stored_class );
	esc_textarea( $text );
	esc_html_e( 'Hello World', 'text_domain' );
	*/

}


function apg_escape_output(){
	
}